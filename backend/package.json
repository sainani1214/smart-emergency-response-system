{
  "name": "backend",
  "version": "1.0.0",
  "description": "Smart Emergency Response System - Backend API",
  "main": "dist/server.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "lint": "tsc --noEmit",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "emergency",
    "response",
    "real-time",
    "fastify",
    "socket.io"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "dotenv": "^17.3.1",
    "fastify": "^5.8.2",
    "mongoose": "^8.8.4",
    "socket.io": "^4.8.3"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.5.0"
  }
}
